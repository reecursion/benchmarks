"""PaperBench benchmark evaluation.

This package provides both multi-agent and single-agent approaches for paper reproduction:

Multi-agent (default):
    python -m benchmarks.paper_bench.run_infer --task-name bam --llm-config-path config.json

Single-agent:
    python -m benchmarks.paper_bench.single_agent.run_infer --task-name bam --llm-config-path config.json
"""
